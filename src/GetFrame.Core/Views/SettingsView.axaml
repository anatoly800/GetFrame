<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="GetFrame.Core.Views.SettingsView"
        Title="Settings"
        Width="400"
        Height="300">
  
  <Grid RowDefinitions="Auto,*">
    <!-- Navigation bar -->
    <Border Grid.Row="0" Padding="10">
      <StackPanel Orientation="Horizontal">
        <Button Name="BackButton" Content="←" Margin="0,0,10,0" VerticalAlignment="Center"/>
        <TextBlock Text="Settings" FontSize="16" VerticalAlignment="Center" FontWeight="Bold"/>
      </StackPanel>
    </Border>

    <!-- Settings content -->
    <ScrollViewer Grid.Row="1" Margin="10">
      <StackPanel Orientation="Vertical" Spacing="10">
        <!-- Theme toggle -->
        <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
          <TextBlock Text="Dark Theme" VerticalAlignment="Center"/>
          <CheckBox Name="ThemeToggle"/>
        </StackPanel>

        <!-- PNG Save Path -->
        <StackPanel>
          <TextBlock Text="PNG Save Path:" Margin="0,0,0,5"/>
          <Grid ColumnDefinitions="*,Auto">
            <TextBox Grid.Column="0" Name="PngSavePathTextBox" IsReadOnly="True"/>
            <Button Grid.Column="1" Name="SelectPngPathButton" Content="..." Margin="5,0,0,0" Padding="10,2"/>
          </Grid>
        </StackPanel>

        <!-- FFmpeg Path (Windows only) -->
        <StackPanel Name="FfmpegPathPanel">
          <TextBlock Text="FFmpeg Path:" Margin="0,0,0,5"/>
          <Grid ColumnDefinitions="*,Auto">
            <TextBox Grid.Column="0" Name="FfmpegPathTextBox" IsReadOnly="True"/>
            <Button Grid.Column="1" Name="SelectFfmpegPathButton" Content="..." Margin="5,0,0,0" Padding="10,2"/>
          </Grid>
        </StackPanel>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</Window>